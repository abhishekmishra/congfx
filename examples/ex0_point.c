#include "congfx.h"

void setup()
{
    // background(12);
    // stroke(15);
}

void draw(cg_uint dt)
{
    background(11);
    stroke(14);

    // draw a point at the center of the canvas
    point(width / 2, height / 2, L'X');

    // no loop
    // no_loop();
}